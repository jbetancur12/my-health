services:
  postgres:
    restart: unless-stopped

  minio:
    restart: unless-stopped

  app:
    restart: unless-stopped

  web:
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-80}:80"
